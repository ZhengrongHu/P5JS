/*
note: shift and alt don't have visiable character
*/
function setup() {
  createCanvas(240, 120);
  textSize(64); 
  textAlign(CENTER);
  fill(255);
}

function draw() {
  background(204);
  text(key, 60, 80);
  }