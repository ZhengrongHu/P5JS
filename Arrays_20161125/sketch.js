/*
note: 
 avoid creating arrays with draw(), as creating a new array on every frame will
 slow down your frame rate
 */
 
var x = []; // declare the array

function setup(){
  createCanvas(200, 200);
  x[0] = 12;// assign the first value
  x[i] = 2; // assign the second value
}

var x = [12, 2, 12]; // declare and assign 

