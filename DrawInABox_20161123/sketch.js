/*
note: 
  
*/

var quote = "one small step for man,,,";

function setup() {
  createCanvas(480, 400);
  textFont("Arial");
  textSize(24);
}

function draw() {
  background(102); 
  text("one small step for pusheen", 25, 24, 240, 100); 
  
}